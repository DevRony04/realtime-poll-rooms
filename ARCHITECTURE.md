# 🏗️ Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT BROWSER                            │
│                                                                  │
│  ┌────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   Create Poll  │  │   View/Vote     │  │  Real-time UI   │ │
│  │   (page.tsx)   │  │  ([pollId]/     │  │   Updates       │ │
│  │                │  │   page.tsx)     │  │                 │ │
│  └────────┬───────┘  └────────┬────────┘  └────────┬────────┘ │
│           │                   │                      │          │
│           │                   │                      │          │
│  ┌────────▼───────────────────▼──────────────────────▼────────┐ │
│  │         React Components + State Management               │ │
│  │  (useState, useEffect, form handling, toast, etc.)       │ │
│  └────────┬───────────────────┬──────────────────────┬────────┘ │
│           │                   │                      │          │
│           │ HTTP              │ HTTP                 │ WebSocket│
│           │ POST/GET          │ POST                 │ Subscribe│
└───────────┼───────────────────┼──────────────────────┼──────────┘
            │                   │                      │
            │                   │                      │
┌───────────▼───────────────────▼──────────────────────▼──────────┐
│                        NEXT.JS SERVER                            │
│                      (Vercel/Node.js)                           │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    API ROUTES (REST)                      │  │
│  │                                                            │  │
│  │  POST /api/polls                                          │  │
│  │  ├─ Validate input                                        │  │
│  │  ├─ Create poll with options (transaction)               │  │
│  │  └─ Return poll ID + data                                │  │
│  │                                                            │  │
│  │  GET /api/polls/[pollId]                                  │  │
│  │  ├─ Fetch poll with vote counts                          │  │
│  │  ├─ Calculate percentages                                │  │
│  │  └─ Return poll data                                     │  │
│  │                                                            │  │
│  │  POST /api/polls/[pollId]/vote                           │  │
│  │  ├─ Extract IP address (Anti-abuse #1)                   │  │
│  │  ├─ Verify fingerprint (Anti-abuse #2)                   │  │
│  │  ├─ Check for duplicate votes (IP + fingerprint)         │  │
│  │  ├─ Create vote record                                   │  │
│  │  ├─ Get updated poll results                             │  │
│  │  └─ Trigger Pusher broadcast ───────────────────┐       │  │
│  └────────────────────────┬─────────────────────────┼───────┘  │
│                           │                         │          │
│  ┌────────────────────────▼─────────────────────────┼───────┐  │
│  │              PRISMA ORM (Type-safe)              │       │  │
│  │  ├─ Connection pooling                           │       │  │
│  │  ├─ Query building                               │       │  │
│  │  ├─ Transaction handling                         │       │  │
│  │  └─ Type generation                              │       │  │
│  └────────────────────────┬─────────────────────────┼───────┘  │
│                           │                         │          │
└───────────────────────────┼─────────────────────────┼──────────┘
                            │                         │
                            │                         │
            ┌───────────────▼──────┐    ┌─────────────▼──────────┐
            │   POSTGRESQL DB      │    │   PUSHER (WebSocket)   │
            │  (Vercel/Supabase)   │    │   Real-time Service    │
            │                      │    │                        │
            │  ┌────────────────┐  │    │  Channel: poll-{id}    │
            │  │  Poll Table    │  │    │  Event: vote-update    │
            │  │  - id          │  │    │                        │
            │  │  - question    │  │    │  Broadcasts to all     │
            │  │  - timestamps  │  │    │  subscribers           │
            │  └────────────────┘  │    │                        │
            │                      │    └─────────────┬──────────┘
            │  ┌────────────────┐  │                  │
            │  │  Option Table  │  │                  │
            │  │  - id          │  │                  │
            │  │  - pollId (FK) │  │                  │
            │  │  - text        │  │                  │
            │  │  - position    │  │                  │
            │  └────────────────┘  │                  │
            │                      │                  │
            │  ┌────────────────┐  │                  │
            │  │  Vote Table    │  │                  │
            │  │  - id          │  │                  │
            │  │  - pollId (FK) │  │                  │
            │  │  - optionId    │  │                  │
            │  │  - ipAddress   │  │◄─────────────────┘
            │  │  - fingerprint │  │   Push updates
            │  │  - userAgent   │  │   to all clients
            │  │  - createdAt   │  │
            │  └────────────────┘  │
            │                      │
            │  Indexes:            │
            │  - (ipAddress, poll) │
            │  - (fingerprint, poll)│
            └──────────────────────┘
```

## Data Flow: Creating a Poll

```
User Input ──► Next.js Form ──► POST /api/polls
                                      │
                                      ├─► Validate input
                                      │
                                      ├─► Prisma Transaction:
                                      │   ├─ Create Poll
                                      │   └─ Create Options
                                      │
                                      ├─► Generate UUID
                                      │
                                      └─► Return poll data
                                           │
                                           ▼
                              Redirect to /poll/[pollId]
```

## Data Flow: Voting (with Real-time Update)

```
User Clicks Vote
      │
      ├─► Generate Fingerprint (FingerprintJS)
      │
      ├─► POST /api/polls/[pollId]/vote
      │        {optionId, fingerprint}
      │
      ▼
Server Receives Request
      │
      ├─► Extract IP from headers
      │
      ├─► Check existing votes:
      │   ├─ Query by IP + pollId
      │   └─ Query by fingerprint + pollId
      │
      ├─► If duplicate ──► Return 409 Conflict
      │
      ├─► Create vote record
      │
      ├─► Fetch updated results
      │
      ├─► Broadcast via Pusher ─────┐
      │   Channel: poll-{id}         │
      │   Event: vote-update          │
      │   Data: {poll with results}   │
      │                               │
      └─► Return success              │
                                      │
                                      ▼
                          All Connected Clients
                                      │
                                      ├─► Receive WebSocket event
                                      │
                                      ├─► Update local state
                                      │
                                      └─► Re-render with new results
                                           (progress bars, counts, %)
```

## Anti-Abuse Architecture

```
Vote Request
      │
      ├─► CLIENT SIDE
      │   └─► FingerprintJS generates unique ID
      │       (browser, OS, fonts, canvas, etc.)
      │
      ▼
Server Receives
      │
      ├─► MECHANISM #1: IP Tracking
      │   ├─ Extract from x-forwarded-for
      │   ├─ Check Vote table
      │   └─ Block if IP already voted
      │
      ├─► MECHANISM #2: Fingerprinting
      │   ├─ Receive fingerprint hash
      │   ├─ Check Vote table
      │   └─ Block if fingerprint already voted
      │
      └─► Both pass ──► Allow vote
          Either fails ──► Return 409 Conflict
```

## Database Relationships

```
┌─────────────┐
│    Poll     │
│  (id, q)    │
└──────┬──────┘
       │ 1
       │
       │ *
┌──────▼──────────┐         ┌──────────────┐
│     Option      │ 1    *  │     Vote     │
│ (id, pollId,    ├─────────┤ (id, pollId, │
│  text, pos)     │         │  optionId,   │
└─────────────────┘         │  ip, finger, │
                            │  userAgent)  │
                            └──────────────┘
```

## Technology Stack Layers

```
┌────────────────────────────────────────────┐
│           PRESENTATION LAYER                │
│  Next.js Pages + React Components           │
│  Tailwind CSS + shadcn/ui                   │
└────────────────┬───────────────────────────┘
                 │
┌────────────────▼───────────────────────────┐
│          APPLICATION LAYER                  │
│  Next.js API Routes (REST)                  │
│  Business Logic + Validation                │
└────────────────┬───────────────────────────┘
                 │
┌────────────────▼───────────────────────────┐
│            DATA ACCESS LAYER                │
│  Prisma ORM                                 │
│  Type-safe queries + Migrations             │
└────────────────┬───────────────────────────┘
                 │
┌────────────────▼───────────────────────────┐
│           PERSISTENCE LAYER                 │
│  PostgreSQL Database                        │
│  Tables + Indexes + Constraints             │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│          REAL-TIME LAYER                    │
│  Pusher WebSocket Service                   │
│  Channel subscription + Broadcasting        │
└────────────────────────────────────────────┘
```

## Deployment Architecture (Vercel)

```
┌────────────────────────────────────────────────┐
│               GLOBAL CDN EDGE                   │
│         (Vercel Edge Network)                   │
│  ┌──────────────────────────────────────────┐  │
│  │  Static Assets (JS, CSS, Images)         │  │
│  │  Cached at edge locations worldwide      │  │
│  └──────────────────────────────────────────┘  │
└────────────────┬───────────────────────────────┘
                 │
┌────────────────▼───────────────────────────────┐
│           SERVERLESS FUNCTIONS                  │
│         (Next.js Server on Vercel)              │
│  ┌──────────────────────────────────────────┐  │
│  │  API Routes (auto-scaled)                │  │
│  │  SSR Pages (rendered on-demand)          │  │
│  └──────────────────────────────────────────┘  │
└─────┬─────────────────────────────────┬────────┘
      │                                 │
      │                                 │
┌─────▼──────────┐              ┌──────▼─────────┐
│  Vercel        │              │   Pusher       │
│  Postgres      │              │   Channels     │
│                │              │                │
│  - Managed DB  │              │  - WebSocket   │
│  - Auto backup │              │  - Real-time   │
│  - Scaling     │              │  - Reliable    │
└────────────────┘              └────────────────┘
```

## Security Layers

```
┌────────────────────────────────────────────┐
│         INPUT VALIDATION                    │
│  - Client-side (React)                      │
│  - Server-side (API routes)                 │
└────────────────┬───────────────────────────┘
                 │
┌────────────────▼───────────────────────────┐
│         ANTI-ABUSE LAYER                    │
│  - IP tracking                              │
│  - Browser fingerprinting                   │
│  - Rate limiting checks                     │
└────────────────┬───────────────────────────┘
                 │
┌────────────────▼───────────────────────────┐
│         ORM PROTECTION                      │
│  - Prisma SQL injection prevention          │
│  - Parameterized queries                    │
│  - Type safety                              │
└────────────────┬───────────────────────────┘
                 │
┌────────────────▼───────────────────────────┐
│         DATABASE LAYER                      │
│  - Constraints                              │
│  - Foreign keys                             │
│  - Transactions                             │
└────────────────────────────────────────────┘
```

## Performance Optimizations

```
┌──────────────────────────────────────────┐
│  CLIENT-SIDE                              │
│  - Code splitting (Next.js automatic)     │
│  - Component lazy loading                 │
│  - Optimistic UI updates                  │
└──────────────┬───────────────────────────┘
               │
┌──────────────▼───────────────────────────┐
│  SERVER-SIDE                              │
│  - SSR for initial load                   │
│  - API route optimization                 │
│  - Database connection pooling            │
└──────────────┬───────────────────────────┘
               │
┌──────────────▼───────────────────────────┐
│  DATABASE                                 │
│  - Indexed queries                        │
│  - Efficient joins                        │
│  - Query optimization                     │
└──────────────────────────────────────────┘
```

## Scaling Considerations

```
Current Setup:
- 1 Vercel serverless instance (auto-scales)
- 1 PostgreSQL database
- 1 Pusher channel per poll

Bottlenecks:
1. Pusher: ~100 concurrent users per channel
2. Database: Connection pool limits
3. Serverless: Cold start latency

Solutions for Scale:
┌──────────────────────────────────────────┐
│  > 1,000 concurrent users                 │
│  - Use Redis for caching                  │
│  - Implement database read replicas       │
│  - Self-hosted WebSocket (Socket.io)      │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│  > 10,000 polls                           │
│  - Add database indexes                   │
│  - Implement pagination                   │
│  - Cache frequently accessed polls        │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│  > 100,000 votes/day                      │
│  - Queue system for vote processing       │
│  - Batch database writes                  │
│  - Sharding strategy                      │
└──────────────────────────────────────────┘
```

---

This architecture provides:
✅ Separation of concerns
✅ Scalability
✅ Security
✅ Real-time capabilities
✅ Type safety
✅ Easy deployment
✅ Monitoring capabilities
